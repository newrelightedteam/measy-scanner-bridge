https://github.com/settings/tokens?type=beta

git init
git branch -M main


git remote add origin https://github.com/newrelightedteam/measy-scanner-bridge.git

git add .
git commit -m "Initial commit: Scanner Bridge with auto-updater and CI build workflow"
git push -u origin main


git tag v1.0.0
git push origin v1.0.0


############################### repush 

    update package.json version

   git add package.json
   git commit -m "Bump version to 1.0.1"
   git tag v1.0.1
   git push origin main
   git push origin v1.0.1